/**
 * @deprecated Use gateway-agent.ts — kept as a thin re-export for older imports.
 */
export {
  GatewayAgent as GatewaySessionAgent,
  GatewayAgent,
  type GatewayAgentState,
} from "./gateway-agent";
